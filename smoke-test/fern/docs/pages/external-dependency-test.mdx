# External Dependency Test

This page tests what happens when we use a component with an npm dependency that isn't installed.

<ExternalDependencyComponent imagePath="/img/test-image.png" caption="Testing external dependency" />

## What's happening?

The component above imports from `@docusaurus/useBaseUrl`, which is not installed in this project.

Let's see what error message appears!
