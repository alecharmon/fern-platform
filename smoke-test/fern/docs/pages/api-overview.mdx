---
title: Tasks API Overview
subtitle: A simple API for managing tasks
slug: api-overview
---

The Tasks API provides a straightforward way to manage tasks in your application. Below you'll find examples of how to interact with the API.

## Create a Task

Use the following endpoint to create a new task:

<EndpointRequestSnippet endpoint="POST /tasks" />

## Task Schema

Here's the schema for a Task object:

<Schema api="api" type="Task" />

## Task Schema (with description)

Here's the same schema, but with the type description included:

<Schema api="api" type="Task" description />

## gRPC Comment Schema (with description)

Here's a Protobuf message schema with description rendered from proto comments:

<Schema api="grpc-api" type="Comment" description />

## gRPC Comment Schema (without description)

Same schema, without the description:

<Schema api="grpc-api" type="Comment" />
