{
  "question": "I want GitHub Actions to check my gRPC definitions on pull requests and automatically publish libraries when I merge to main. Should it run on every PR or only when definition files change? What's the difference between validation and generation commands?",
  "ground_truth": "Use Fern's GitHub Action (`fern-api/action@v0`) to automate SDK and docs updates for your gRPC service. Create a workflow file at `.github/workflows/fern.yml` with two jobs: 1) `fern-check` that runs on pull requests when files in `proto/**/*.proto` or `fern/**/*.yml` paths change (this validates your spec without generating anything), and 2) `fern-generate` that runs only on pushes to main branch (this actually generates and publishes SDKs and docs). Use path filters to trigger workflows only when relevant files change, improving CI efficiency. Both jobs require your `FERN_TOKEN` in secrets. The `check` command validates your API spec for errors without side effects (perfect for PR validation), while `generate` creates and publishes the actual SDK and documentation artifacts (should only run on main to avoid publishing from feature branches). This two-step approach ensures validation on every PR while controlling when artifacts are actually published.",
  "metadata": {
    "category": "markdown",
    "source": "markdown_generator",
    "slug": "automation",
    "cited_context": "Use Fern's GitHub Action to automatically update SDKs and docs when your Protocol Buffer files change:\n\n```yaml title=\".github/workflows/fern.yml\"\nname: Fern\n\non:\n  push:\n    branches:\n      - main\n    paths:\n      - 'proto/**/*.proto'\n      - 'fern/**/*.yml'\n  pull_request:\n    branches:\n      - main\n    paths:\n      - 'proto/**/*.proto'\n      - 'fern/**/*.yml'\n\njobs:\n  fern-check:\n    runs-on: ubuntu-latest\n    steps:\n      - name: Checkout repo\n        uses: actions/checkout@v4\n        \n      - name: Check gRPC spec\n        uses: fern-api/action@v0\n        with:\n          command: check\n        env:\n          FERN_TOKEN: ${{{{ secrets.FERN_TOKEN }}}}\n\n  fern-generate:\n    runs-on: ubuntu-latest\n    if: github.event_name == 'push' && github.ref == 'refs/heads/main'\n    steps:\n      - name: Checkout repo\n        uses: actions/checkout@v4\n        \n      - name: Generate SDKs and docs\n        uses: fern-api/action@v0\n        with:\n          command: generate\n        env:\n          FERN_TOKEN: ${{{{ secrets.FERN_TOKEN }}}}\n```"
  },
  "criteria": {
    "citation": [
      "https://fern-eval.docs.buildwithfern.com/api-definitions/grpc/sync-your-g-rpc-specification"
    ],
    "correctness": [
      "The answer must mention using Fern's GitHub Action.",
      "The answer should contain a code example of how to use Fern's GitHub Action.",
      "The answer should indicate that the workflow should trigger on pushes to main branch when files in `proto/**/*.proto` or `fern/**/*.yml` paths change."
    ]
  }
}
