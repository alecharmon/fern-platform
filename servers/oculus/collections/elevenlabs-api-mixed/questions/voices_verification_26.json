{
  "question": "What does the requires_verification field mean in the voice clone API response?",
  "ground_truth": "When creating a voice clone via POST /v1/voices/add, the response includes a 'requires_verification' boolean field along with the 'voice_id'. This field indicates whether the created voice requires additional verification steps before it can be fully used.",
  "metadata": {
    "category": "voices",
    "source": "documentation",
    "endpoint": "POST /v1/voices/add",
    "question_type": "parameter-specific"
  },
  "criteria": {
    "citation": [
      "https://eleven-labs-indexing.docs.buildwithfern.com/api-reference/voices/ivc/create"
    ],
    "correctness": [
      "Must mention requires_verification field",
      "Must mention it's a boolean in the response",
      "Should mention it's related to voice creation"
    ]
  }
}