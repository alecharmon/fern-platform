{
  "question": "How do I control which versions appear in different documentation instances?",
  "ground_truth": "You can control which versions appear in each documentation instance by tagging them with audiences. Define audiences for instances and versions in your docs.yml file. Content is filtered based on audience tags - content with matching audiences is included, non-matching audiences are excluded, and content without audience tags is included by default. This enables separate sites for different user groups like internal developers, beta testers, or public customers.",
  "metadata": {
    "category": "markdown",
    "source": "markdown_generator",
    "slug": "versions",
    "domain": "buildwithfern.com",
    "cited_context": "Control which versions appear in each [documentation instance](/docs/configuration/what-is-docs-yml#instances-configuration) by tagging them with audiences. This enables separate sites for different user groups (e.g., internal developers, beta testers, public customers).\n\nContent is filtered based on audience tags:\n\n- **Match**: Content with an audience matching the instance audience is included\n- **No match**: Content with a non-matching audience is excluded  \n- **No audience**: Content without an audience tag is included by default\n\nDefine audiences for instances and versions in `docs.yml`"
  }
}