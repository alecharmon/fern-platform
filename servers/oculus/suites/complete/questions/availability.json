{
    "question": "How can I mark an entire section of my API Reference documentation as beta in Fern?",
    "ground_truth": "You can mark an entire section of your API Reference documentation as beta by setting the `availability` property to `beta` for that section in your `docs.yml` configuration file. When you set the availability of a section, all endpoints within that section are automatically marked with that availability unless explicitly set otherwise.",
    "metadata": {
      "category": "markdown",
      "source": "markdown_generator",
      "slug": "availability",
      "domain": "buildwithfern.com",
      "cited_context": "You can set the availability for the entire API reference or for specific sections in your `docs.yml` configuration. Options are: `stable`, `generally-available`, `in-development`, `pre-release`, `deprecated`, or `beta`. When you set the availability of a section, all of the endpoints in that section are automatically marked with that availability unless explicitly set otherwise."
    },
    "criteria": {
        "citation": [
          "https://buildwithfern.com/learn/api-definitions/openapi/extensions/availability"
        ],
        "correctness": [
          "The answer must mention the `availability` property in the `docs.yml` file.",
          "The answer must mention the `beta` value for the `availability` property.",
          "The answer should mention all endpoints within the marked section are automatically marked with that availability.",
          "The answer should include a code example of how to use the `availability` property."
        ]
      }
  }