{
  "question": "What is the difference between the liveness probe and readiness probe endpoints in the self-hosted container?",
  "ground_truth": "The liveness probe at /liveness detects unrecoverable failures by checking if critical service processes are still running via their PIDs, and triggers container restart when processes have died. The readiness probe at /readiness prevents traffic routing to containers that are still starting up by verifying that all services are healthy and ready to serve traffic through their health endpoints.",
  "metadata": {
    "category": "markdown",
    "source": "markdown_generator",
    "slug": "health-check-endpoints",
    "domain": "buildwithfern.com",
    "cited_context": "The **liveness probe** detects unrecoverable failures and triggers container restart, while the **readiness probe** prevents traffic routing to containers that are still starting up. By using both probes, deployments no longer need arbitrary timeouts like `--wait --timeout 15m0s`.\n\n### Liveness probe\n\n`GET /liveness` verifies that all critical service processes are still running by checking their PIDs. Use this to distinguish between unrecoverable failures (process died) and slow startups.\n\n### Readiness probe\n\n`GET /readiness` verifies that all services are healthy and ready to serve traffic by testing their health endpoints. It returns success only when all services are fully initialized and responsive."
  },
  "criteria": null
}