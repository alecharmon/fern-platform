// This file was auto-generated by Fern from our API Definition.

import express from "express";
import * as errors from "../../../../../../../../errors/index";
import type * as FernRegistry from "../../../../../../../index";

export interface WriteServiceMethods {
    startDocsRegister(
        req: express.Request<
            never,
            FernRegistry.docs.v1.write.StartDocsRegisterResponse,
            FernRegistry.docs.v1.write.StartDocsRegisterRequest,
            never
        >,
        res: {
            send: (responseBody: FernRegistry.docs.v1.write.StartDocsRegisterResponse) => Promise<void>;
            cookie: (cookie: string, value: string, options?: express.CookieOptions) => void;
            locals: any;
        },
        next: express.NextFunction,
    ): void | Promise<void>;
    finishDocsRegister(
        req: express.Request<
            {
                docsRegistrationId: FernRegistry.docs.v1.write.DocsRegistrationId;
            },
            never,
            FernRegistry.docs.v1.write.RegisterDocsRequest,
            never
        >,
        res: {
            send: () => Promise<void>;
            cookie: (cookie: string, value: string, options?: express.CookieOptions) => void;
            locals: any;
        },
        next: express.NextFunction,
    ): void | Promise<void>;
}

export class WriteService {
    private router;

    constructor(
        private readonly methods: WriteServiceMethods,
        middleware: express.RequestHandler[] = [],
    ) {
        this.router = express.Router({ mergeParams: true }).use(
            express.json({
                strict: false,
            }),
            ...middleware,
        );
    }

    public addMiddleware(handler: express.RequestHandler): this {
        this.router.use(handler);
        return this;
    }

    public toRouter(): express.Router {
        this.router.post("/init", async (req, res, next) => {
            try {
                await this.methods.startDocsRegister(
                    req as any,
                    {
                        send: async (responseBody) => {
                            res.json(responseBody);
                        },
                        cookie: res.cookie.bind(res),
                        locals: res.locals,
                    },
                    next,
                );
                if (!res.writableEnded) {
                    next();
                }
            } catch (error) {
                if (error instanceof errors.FernRegistryError) {
                    console.warn(
                        `Endpoint 'startDocsRegister' unexpectedly threw ${error.constructor.name}. If this was intentional, please add ${error.constructor.name} to the endpoint's errors list in your Fern Definition.`,
                    );
                    await error.send(res);
                } else {
                    res.status(500).json("Internal Server Error");
                }
                next(error);
            }
        });
        this.router.post("/register/:docsRegistrationId", async (req, res, next) => {
            try {
                await this.methods.finishDocsRegister(
                    req as any,
                    {
                        send: async () => {
                            res.sendStatus(204);
                        },
                        cookie: res.cookie.bind(res),
                        locals: res.locals,
                    },
                    next,
                );
                if (!res.writableEnded) {
                    next();
                }
            } catch (error) {
                if (error instanceof errors.FernRegistryError) {
                    switch (error.errorName) {
                        case "UnauthorizedError":
                        case "UserNotInOrgError":
                        case "DocsRegistrationIdNotFound":
                            break;
                        default:
                            console.warn(
                                `Endpoint 'finishDocsRegister' unexpectedly threw ${error.constructor.name}. If this was intentional, please add ${error.constructor.name} to the endpoint's errors list in your Fern Definition.`,
                            );
                    }
                    await error.send(res);
                } else {
                    res.status(500).json("Internal Server Error");
                }
                next(error);
            }
        });
        return this.router;
    }
}
