// This file was auto-generated by Fern from our API Definition.

import express from "express";
import * as errors from "../../../../../../errors/index";
import type * as FernRegistry from "../../../../../index";

export interface VersionsServiceMethods {
    computeSemanticVersion(
        req: express.Request<
            never,
            FernRegistry.sdks.ComputedSemanticVersionResponse,
            FernRegistry.sdks.ComputeSemanticVersionRequest,
            never
        >,
        res: {
            send: (responseBody: FernRegistry.sdks.ComputedSemanticVersionResponse) => Promise<void>;
            cookie: (cookie: string, value: string, options?: express.CookieOptions) => void;
            locals: any;
        },
        next: express.NextFunction,
    ): void | Promise<void>;
}

export class VersionsService {
    private router;

    constructor(
        private readonly methods: VersionsServiceMethods,
        middleware: express.RequestHandler[] = [],
    ) {
        this.router = express.Router({ mergeParams: true }).use(
            express.json({
                strict: false,
            }),
            ...middleware,
        );
    }

    public addMiddleware(handler: express.RequestHandler): this {
        this.router.use(handler);
        return this;
    }

    public toRouter(): express.Router {
        this.router.post("/semantic-version/compute", async (req, res, next) => {
            try {
                await this.methods.computeSemanticVersion(
                    req as any,
                    {
                        send: async (responseBody) => {
                            res.json(responseBody);
                        },
                        cookie: res.cookie.bind(res),
                        locals: res.locals,
                    },
                    next,
                );
                if (!res.writableEnded) {
                    next();
                }
            } catch (error) {
                if (error instanceof errors.FernRegistryError) {
                    switch (error.errorName) {
                        case "FailedToComputeExistingVersion":
                        case "FailedToIncrementVersion":
                            break;
                        default:
                            console.warn(
                                `Endpoint 'computeSemanticVersion' unexpectedly threw ${error.constructor.name}. If this was intentional, please add ${error.constructor.name} to the endpoint's errors list in your Fern Definition.`,
                            );
                    }
                    await error.send(res);
                } else {
                    res.status(500).json("Internal Server Error");
                }
                next(error);
            }
        });
        return this.router;
    }
}
