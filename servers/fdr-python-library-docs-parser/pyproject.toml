[tool.poetry]
name = "fdr-python-library-docs-parser"
version = "0.1.0"
description = "Lambda for parsing Python library documentation"

[tool.poetry.dependencies]
python = "^3.12"
boto3 = "^1.34"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
