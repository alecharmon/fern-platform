# pdf-gen

Minimal local server app scaffold for PDF generation work.

## Local usage

```bash
pnpm -w install
pnpm -C servers/pdf-gen playwright:install
pnpm -C servers/pdf-gen dev:generate-docs-pdf
pnpm -C servers/pdf-gen dev:generate-cover-pdf
```
