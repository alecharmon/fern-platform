name: Deploy Fern Docs
env:
  VERCEL_ORG_ID: team_6FKOM5nw037hv8g2mTk3gaH7
  TURBO_TOKEN: ${{ secrets.TURBO_TOKEN }}
  TURBO_TEAM: "buildwithfern"
on:
  push:
    tags:
      - "docs@*"
jobs:
  Deploy-Production:
    runs-on: ubuntu-latest
    env:
      VERCEL_PROJECT_ID: prj_XXWllSgR38im0A8e0cKXjOnMoo07
      NODE_OPTIONS: --max-old-space-size=8192
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 2 # This helps turbo resolve changes faster
      - name: Install
        uses: ./.github/actions/install
      - name: Install Vercel CLI
        run: npm install --global vercel@latest
      - name: Pull Vercel Environment Information
        run: vercel pull --yes --environment=production --token=${{ secrets.VERCEL_TOKEN }}
      - name: Build Project Artifacts
        run: vercel build --prod --token=${{ secrets.VERCEL_TOKEN }}
      - name: Deploy Project Artifacts to Vercel
        id: deploy
        run: |
          url=$(vercel deploy --prebuilt --prod --token=${{ secrets.VERCEL_TOKEN }})
          echo "VERCEL_DEPLOYMENT_URL=$url" >> $GITHUB_OUTPUT
      - name: Promote Deployment
        run: |
          url="${{ steps.deploy.outputs.VERCEL_DEPLOYMENT_URL }}"
          output=$(vercel promote $url --scope=buildwithfern --token=${{ secrets.VERCEL_TOKEN }} 2>&1) || {
            if echo "$output" | grep -q "(409)"; then
              echo "Deployment is already the current production deployment. Skipping promote."
            else
              echo "$output"
              exit 1
            fi
          }

  Deploy-Production-Slash:
    runs-on: ubuntu-latest
    env:
      VERCEL_PROJECT_ID: prj_TnhxLeFgHnzYBZV6XfKMZ8sfUd13
      NODE_OPTIONS: --max-old-space-size=8192
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 2 # This helps turbo resolve changes faster
      - name: Install
        uses: ./.github/actions/install
      - name: Install Vercel CLI
        run: npm install --global vercel@latest
      - name: Pull Vercel Environment Information
        run: vercel pull --yes --environment=production --token=${{ secrets.VERCEL_TOKEN }}
      - name: Build Project Artifacts
        run: vercel build --prod --token=${{ secrets.VERCEL_TOKEN }}
      - name: Deploy Project Artifacts to Vercel
        id: deploy
        run: |
          url=$(vercel deploy --prebuilt --prod --token=${{ secrets.VERCEL_TOKEN }})
          echo "VERCEL_DEPLOYMENT_URL=$url" >> $GITHUB_OUTPUT
      - name: Promote Deployment
        run: |
          url="${{ steps.deploy.outputs.VERCEL_DEPLOYMENT_URL }}"
          output=$(vercel promote $url --scope=buildwithfern --token=${{ secrets.VERCEL_TOKEN }} 2>&1) || {
            if echo "$output" | grep -q "(409)"; then
              echo "Deployment is already the current production deployment. Skipping promote."
            else
              echo "$output"
              exit 1
            fi
          }

  Deploy-Production-Assets:
      runs-on: ubuntu-latest
      env:
        VERCEL_PROJECT_ID: prj_PScht6A7acGj2jELnQHXx3NLPVzw
        NODE_OPTIONS: --max-old-space-size=8192
      steps:
        - uses: actions/checkout@v6
          with:
            fetch-depth: 2 # This helps turbo resolve changes faster
        - name: Install
          uses: ./.github/actions/install
        - name: Install Vercel CLI
          run: npm install --global vercel@latest
        - name: Pull Vercel Environment Information
          run: vercel pull --yes --environment=production --token=${{ secrets.VERCEL_TOKEN }}
        - name: Build Project Artifacts
          run: vercel build --prod --token=${{ secrets.VERCEL_TOKEN }}
        - name: Deploy Project Artifacts to Vercel
          id: deploy
          run: |
            url=$(vercel deploy --prebuilt --prod --token=${{ secrets.VERCEL_TOKEN }})
            echo "VERCEL_DEPLOYMENT_URL=$url" >> $GITHUB_OUTPUT
        - name: Promote Deployment
          run: |
            url="${{ steps.deploy.outputs.VERCEL_DEPLOYMENT_URL }}"
            output=$(vercel promote $url --scope=buildwithfern --token=${{ secrets.VERCEL_TOKEN }} 2>&1) || {
              if echo "$output" | grep -q "(409)"; then
                echo "Deployment is already the current production deployment. Skipping promote."
              else
                echo "$output"
                exit 1
              fi
            }

  Deploy-Production-Preview:
      runs-on: ubuntu-latest
      env:
        VERCEL_PROJECT_ID: prj_z06epFqkoFgm4PjgtDfcKLmXbWrW
        NODE_OPTIONS: --max-old-space-size=8192
      steps:
        - uses: actions/checkout@v6
          with:
            fetch-depth: 2 # This helps turbo resolve changes faster
        - name: Install
          uses: ./.github/actions/install
        - name: Install Vercel CLI
          run: npm install --global vercel@latest
        - name: Pull Vercel Environment Information
          run: vercel pull --yes --environment=production --token=${{ secrets.VERCEL_TOKEN }}
        - name: Build Project Artifacts
          run: vercel build --prod --token=${{ secrets.VERCEL_TOKEN }}
        - name: Deploy Project Artifacts to Vercel
          id: deploy
          run: |
            url=$(vercel deploy --prebuilt --prod --token=${{ secrets.VERCEL_TOKEN }})
            echo "VERCEL_DEPLOYMENT_URL=$url" >> $GITHUB_OUTPUT
        - name: Promote Deployment
          run: |
            url="${{ steps.deploy.outputs.VERCEL_DEPLOYMENT_URL }}"
            output=$(vercel promote $url --scope=buildwithfern --token=${{ secrets.VERCEL_TOKEN }} 2>&1) || {
              if echo "$output" | grep -q "(409)"; then
                echo "Deployment is already the current production deployment. Skipping promote."
              else
                echo "$output"
                exit 1
              fi
            }

  Deploy-Production-Remote-Renderer:
    runs-on: ubuntu-latest
    env:
      VERCEL_PROJECT_ID: prj_usmMkERz2Bv8P3pNWYABszQXDLFj
      NODE_OPTIONS: --max-old-space-size=8192
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 2
      - name: Install
        uses: ./.github/actions/install
      - name: Install Vercel CLI
        run: npm install --global vercel@latest
      - name: Pull Vercel Environment Information
        run: vercel pull --yes --environment=production --token=${{ secrets.VERCEL_TOKEN }}
      - name: Build Project Artifacts
        run: vercel build --prod --token=${{ secrets.VERCEL_TOKEN }}
      - name: Deploy Project Artifacts to Vercel
        id: deploy
        run: |
          url=$(vercel deploy --prebuilt --prod --token=${{ secrets.VERCEL_TOKEN }})
          echo "VERCEL_DEPLOYMENT_URL=$url" >> $GITHUB_OUTPUT
      - name: Promote Deployment
        run: |
          url="${{ steps.deploy.outputs.VERCEL_DEPLOYMENT_URL }}"
          output=$(vercel promote $url --scope=buildwithfern --token=${{ secrets.VERCEL_TOKEN }} 2>&1) || {
            if echo "$output" | grep -q "(409)"; then
              echo "Deployment is already the current production deployment. Skipping promote."
            else
              echo "$output"
              exit 1
            fi
          }

  revalidate-all-sites:
    needs: [Deploy-Production, Deploy-Production-Slash, Deploy-Production-Assets, Deploy-Production-Preview, Deploy-Production-Remote-Renderer]
    uses: ./.github/workflows/revalidate-all-sites.yml
    secrets: inherit
