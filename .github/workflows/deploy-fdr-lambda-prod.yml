name: Deploy FDR Lambda Prod

on:
  push:
    tags: ["fdr-lambda@*"]

env:
  PACKAGE_NAME: "@fern-platform/fdr-lambda"
  TURBO_TOKEN: ${{ secrets.TURBO_TOKEN }}
  TURBO_TEAM: "buildwithfern"
  GITHUB_TOKEN: ${{ secrets.FERN_SUPPORT_GH_ACTIONS_PAT }}
  DATABASE_URL: ${{ secrets.PROD_RDS_PROXY_URL }}
  VENUS_URL: ${{ secrets.VENUS_URL }}
  PUBLIC_DOCS_CDN_URL: ${{ secrets.PUBLIC_DOCS_CDN_URL }}
  PUBLIC_DOCS_S3_BUCKET_NAME: ${{ secrets.PUBLIC_DOCS_S3_BUCKET_NAME }}
  PUBLIC_DOCS_S3_BUCKET_REGION: ${{ secrets.PUBLIC_DOCS_S3_BUCKET_REGION }}
  PRIVATE_DOCS_S3_BUCKET_NAME: ${{ secrets.PRIVATE_DOCS_S3_BUCKET_NAME }}
  PRIVATE_DOCS_S3_BUCKET_REGION: ${{ secrets.PRIVATE_DOCS_S3_BUCKET_REGION }}
  DB_DOCS_DEFINITION_BUCKET_NAME: ${{ secrets.DB_DOCS_DEFINITION_BUCKET_NAME }}
  DB_DOCS_DEFINITION_BUCKET_REGION: ${{ secrets.DB_DOCS_DEFINITION_BUCKET_REGION }}
  JWT_SECRET_KEY: ${{ secrets.PROD_JWT_SECRET_KEY }}

jobs:
  deploy_prod:
    runs-on: Seed
    permissions:
      id-token: write
      contents: read
    if: github.ref_type == 'tag' && github.event_name == 'push' && startsWith(github.ref, 'refs/tags/fdr-lambda@')
    environment: Fern Prod
    strategy:
      matrix:
        concurrency: [1]
    env:
      APPLICATION_ENVIRONMENT: prod
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: 📥 Install
        uses: ./.github/actions/install

      - name: 🔨 Build dependencies
        run: |
          # Build fdr-sdk first so tests can import it
          pnpm compile

      - name: 🧪 Run Tests
        run: |
          cd servers/fdr-lambda
          pnpm test

      - name: 🔨 Build Lambda
        run: |
          pnpm turbo build --filter=${{ env.PACKAGE_NAME }}

      - name: 💻 Setup AWS CLI
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: "arn:aws:iam::985111089818:role/github-ci"
          aws-region: us-east-1

      - name: 🚀 Deploy to Production
        run: |
          git_version="$(scripts/git-version.sh)"
          npm install -g aws-cdk
          cd servers/fdr-lambda-deploy
          VERSION="${git_version}" pnpm --filter=@fern-platform/fdr-lambda-deploy deploy:prod
