{
  "name": "@fern-platform/entitlements",
  "version": "0.0.0",
  "private": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/fern-api/fern-platform.git",
    "directory": "packages/commons/entitlements"
  },
  "sideEffects": false,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "check-types": "tsc --noEmit",
    "clean": "rm -rf ./dist && tsc --build --clean",
    "compile": "tsc",
    "depcheck": "knip",
    "test": "vitest --run --passWithNoTests --globals"
  },
  "dependencies": {
    "@fern-platform/billing": "workspace:*",
    "@fern-platform/supabase": "workspace:*",
    "auth0": "^4.20.0"
  },
  "devDependencies": {
    "@fern-platform/configs": "workspace:*",
    "neverthrow": "^8.2.0",
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
