endpoint_input:
  id: circular-reference-endpoint
  method: POST
  path:
    - type: literal
      value: /api/tree
  requests:
    - contentType: application/json
      body:
        type: object
        extends: []
        properties:
          - key: tree
            valueShape:
              type: id
              id: TreeNode
endpoint_definitions:
  types:
    TreeNode:
      description: A tree node that can have child nodes
      shape:
        type: object
        properties:
          - key: value
            valueShape:
              type: primitive
              value:
                type: string
          - key: children
            valueShape:
              type: optional
              shape:
                type: list
                itemShape:
                  type: id
                  id: TreeNode
output:
  openapi: 3.1.0
  info:
    title: API
    version: 1.0.0
  paths:
    /api/tree:
      post:
        operationId: circular-reference-endpoint
        summary: circular-reference-endpoint
        responses:
          "200":
            description: Successful response
        requestBody:
          content:
            application/json:
              schema:
                type: object
                properties:
                  tree:
                    $ref: "#/components/schemas/TreeNode"
                required:
                  - tree
  components:
    schemas:
      TreeNode:
        type: object
        properties:
          value:
            type: string
          children:
            type: array
            items:
              $ref: "#/components/schemas/TreeNode"
        required:
          - value
        description: A tree node that can have child nodes
