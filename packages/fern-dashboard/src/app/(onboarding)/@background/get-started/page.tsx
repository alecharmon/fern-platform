import DefaultBackgroundSlot from "../default";

/**
 * Background content for the get-started flow.
 * Reuse the shared default background so it renders when the card centers.
 */
export default function GetStartedBackgroundSlot() {
    return <DefaultBackgroundSlot />;
}
